# 论文方法设计稿草案

## 暂定题目

面向自然语言视频检索的错误驱动自学习智能体：结合细粒度对齐、课程式反馈蒸馏与安全闭环优化

## 一、问题背景

当前文本视频检索系统在基于 OpenCLIP ViT-H-14 的强基线之上，已经通过多帧、多视角和 alignment teacher 将锁定 1kA q200 的 `R@1` 推升到 `50.5`。但继续依赖通用 teacher distillation、全量 memory replay 和统一 adapter 微调时，性能提升开始趋于停滞，尤其表现为：

- `R@5/R@10` 偶尔上升，但 `R@1` 不稳定
- continual learning 候选往往被 gate 拒绝
- failure diagnosis 显示错误主要集中在 `object / relation / person_attribute / action`

这说明当前瓶颈已经从“粗召回不足”转向“细粒度跨模态对齐与 top1 校准不足”。

## 二、核心研究问题

1. 如何在不引入数据泄露的前提下，让检索系统从检索错误和正确 gt 之间持续学习？
2. 如何让 continual learning 不只改善 MnR，而能稳定带动 `R@1/R@5/R@10`？
3. 如何让系统在有限算力下保持工程可运行，同时具备论文层面的算法创新性？

## 三、方法主线

本文方法由三个层次组成。

### 3.1 强基线检索层

以当前最优 v3.5 检索器为主干，包括：

- ViT-H-14 文本视频双塔检索
- gated residual adapter
- 多帧 alignment teacher
- 多视角视频特征
- memory / alignment bonus

该层提供稳定的基础召回能力与可重复的锁定评测结果。

### 3.2 错误驱动课程学习层

为避免全量伪监督噪声，我们不再平均学习全部样本，而是只从高价值错误样本中构建反馈 teacher。

具体做法：

1. 用当前最优模型在 `safe_train` 上检索，得到每条 query 的 gt rank。
2. 只保留 `rank 2-30` 的失败样本。
3. 为这些失败样本构建 feedback memory 与 feedback teacher supervision。
4. 采用 warm-start 方式从当前最佳 checkpoint 继续训练 candidate。

设计动机：

- `rank 2-30` 样本说明 gt 已进入可召回范围，适合做 top1 校准
- `rank >30` 的长尾错误噪声更大，直接学习容易拉偏模型

### 3.3 细粒度对齐校准层

针对 failure diagnosis 中的主要错误类型，本文重点增强三类 discrepancy：

- vision discrepancy：对象、人物属性、多视角线索不足
- language discrepancy：query 中属性、关系、动作词的表达粒度与视频表征不匹配
- alignment discrepancy：文本语义片段与视频局部证据之间缺乏稳定对齐

具体实现方向：

- 多帧 alignment teacher
- 多视角视频向量
- component-aware teacher supervision
- pairwise / listwise 候选分离训练

## 四、算法创新点

本文拟将创新点集中在一条主线上，而非简单堆叠模块。

### 创新点 1：错误驱动课程式反馈蒸馏

区别于传统全量蒸馏，本文提出：

- 先根据 gt rank 划分错误难度
- 仅对 near-failure 样本进行 feedback distillation
- 将自学习目标从“全量拟合 teacher”改为“优先解决可纠正错误”

这一点直接对应当前系统的真实瓶颈，也更符合 agent 自学习逻辑。

### 创新点 2：面向 top1 校准的细粒度对齐增强

针对 `object / relation / person_attribute` 主导错误，本文不再只提升宽召回，而是通过：

- component alignment
- one-to-many candidate comparison
- alignment teacher bonus

来提升 top1 候选与近邻错误候选之间的分离能力。

### 创新点 3：安全闭环式 continual agent

本文不是简单训练一个静态检索模型，而是构建一个具备以下能力的检索 agent：

- 从 safe_train 构建反馈监督
- 在 safe_dev 上进行晋升判断
- 自动拒绝无正向增益的学习轮次
- 保持 1kA 仅用于锁定汇报

这使系统同时具备工程闭环和研究价值。

## 五、系统贡献

系统层面可明确写为：

1. 构建了防数据泄露的 `safe_train / safe_dev / 1kA` 三段式协议。
2. 实现了可交互的视频检索前端，支持 baseline / rewrite / rerank、历史检索、反馈记录和学习日志展示。
3. 实现了 error-driven continual loop，使检索系统具备可审计、可拒绝、可追踪的自学习闭环。

## 六、实验设计建议

### 6.1 主实验

- Baseline：当前强基线 v3.5
- + alignment teacher
- + multiview
- + error-driven feedback teacher
- + continual promotion gate

### 6.2 消融实验

- 是否只学 `rank 2-30`
- 是否使用 alignment teacher
- 是否使用 multiview features
- 是否 warm-start
- 是否启用 acceptance gate

### 6.3 自学习有效性评估

自学习 agent 是否有效，至少看以下五项：

1. fixed reference set 不下降
2. feedback set 指标逐轮改善
3. accepted memory 数量增加但 uncertainty 不明显恶化
4. accepted gt rank 均值下降
5. 最终带动 `R@5/R@10`，并逐步影响 `R@1`

## 七、相关论文支撑

- DiscoVLA, CVPR 2025：强调同时处理 vision / language / alignment discrepancy
- TokenBinder, WACV 2025：强调 one-to-many candidate comparison
- BLiM + CPN, ICCV 2025：说明 candidate prior bias 需要校准
- MoE-Adapters4CL, CVPR 2024：说明 continual VLM 适合参数高效专家扩展
- C-CLIP, ICLR 2025：强调 continual VLM 需要同时评估跨模态匹配与遗忘
- Mind the Gap, ICCV 2025：强调 modality gap preservation 对持续学习很重要

## 八、当前结论

目前项目已经具备论文启动条件，建议后续按以下顺序推进：

1. 固定 v3.5 为当前系统强基线
2. 跑通错误驱动自学习闭环
3. 形成 1 组主结果 + 1 组消融 + 1 组自学习曲线
4. 开始论文正文撰写
