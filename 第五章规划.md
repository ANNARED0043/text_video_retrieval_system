1. What changed

把第5章按“**实验结果为中心、主创新优先展示、章节之间分工清晰**”的思路重写了一版，并且同步补齐了：

- 更合理的第5章目录
- 可直接落稿的正文
- 至少 5 篇新增论文引用
- 每张图和每张表的插入位置
- 可直接在终端运行的绘图代码

这次我对原目录做了一个**必要但不大的调整**：  
把你真正的主创新 `alignment + multiview + error-driven loop` 单独拉成实验主线，不再只停留在 baseline / rewrite / rerank。这样第5章才和第3章、第4章的主线一致，也更容易通过审稿。

2. Files modified

这一步是论文正文、图表与引用方案输出，**没有修改仓库文件**。

3. How to run

下方所有绘图代码都按 PowerShell 终端可直接运行的形式给出。默认输出目录：

- `outputs/figures/`

如果本地缺少 `matplotlib`，先安装一次：

```powershell
pip install matplotlib
```

---

# 第5章建议目录

## 第5章 系统测试与分析
### 5.1 实验设置
#### 5.1.1 数据集与测试协议
#### 5.1.2 评价指标设置
#### 5.1.3 主要实验参数设置

### 5.2 Baseline 检索实验结果分析
#### 5.2.1 不同 pooling 方法与聚合策略对比
#### 5.2.2 不同 Top-K 设置与候选召回分析
#### 5.2.3 Baseline 检索性能分析
#### 5.2.4 Baseline 时延分析

### 5.3 系统型增强实验分析
#### 5.3.1 Rewrite All 与 Baseline 对比
#### 5.3.2 Rewrite Selective 与 Baseline 对比
#### 5.3.3 Rewrite + Rerank 组合效果分析
#### 5.3.4 改写比例、时延与 token 成本分析

### 5.4 细粒度表征增强实验分析
#### 5.4.1 表征增强实验设计与阶段划分
#### 5.4.2 Alignment Teacher 的增益分析
#### 5.4.3 Multiview Features 的增益分析
#### 5.4.4 Locked 1kA 主结果分析

### 5.5 错误驱动自学习闭环实验分析
#### 5.5.1 Failure Diagnosis 与反馈样本质量分析
#### 5.5.2 Safe-dev 闭环增益分析
#### 5.5.3 Locked 1kA 验证结果分析
#### 5.5.4 为什么持续学习增益尚未稳定迁移

### 5.6 案例分析与误差分析
#### 5.6.1 典型成功案例分析
#### 5.6.2 查询改写有效案例分析
#### 5.6.3 细粒度增强有效案例分析
#### 5.6.4 失败案例与原因分析

### 5.7 本章小结

---

# 第5章正文

## 5.1 实验设置

### 5.1.1 数据集与测试协议

本文所有实验均基于 MSR-VTT 数据集展开。为避免训练、验证和测试过程之间的信息混淆，系统采用 `safe_train / safe_dev / 1kA` 三段式协议。其中，`safe_train` 仅用于 teacher supervision 构建和 candidate 学习，`safe_dev` 仅用于模型选择与持续学习闭环验证，`1kA` 仅用于最终锁定汇报。这样的协议设置能够有效避免“在开发集上有效但在锁定测试集上失真”的现象，也符合近年来视觉语言持续学习研究对 protocol hygiene 的强调[12][17]。

需要特别说明的是，本文最终论文中的核心 recall 数值变化，均以 `1kA` 协议为标准汇报；`safe_dev` 只承担模型选择与闭环判断作用，而不参与最终主结果比较。该设置与第3章、第4章的方法与系统设计保持一致，也保证了后续实验结论的可解释性。

### 5.1.2 评价指标设置

本文主要采用 Recall@1、Recall@5、Recall@10、Median Rank（MedR）与 Mean Rank（MnR）评价检索效果。其中，\(R@1\) 反映系统能否将正确视频直接排在首位，是最核心的主指标；\(R@5\) 与 \(R@10\) 则用于衡量候选覆盖范围和排序宽容度；MedR 与 MnR 则从整体排名角度反映排序质量。  
近年来针对细粒度视频文本检索的研究普遍指出，仅看宽召回不足以说明模型是否真正学会了细粒度区分，特别是在多个近邻候选高度相似的场景下，\(R@1\) 往往更能体现模型对局部语义差异的判别能力[8][13][14]。因此，本文在结果分析中始终以 \(R@1\) 为主，以 \(R@5\)、\(R@10\)、MedR 和 MnR 为辅助。

### 5.1.3 主要实验参数设置

在 baseline 检索阶段，本文采用 ViT-H-14 / LAION2B_S32B_B79K 作为主要视觉语言编码底座，并在 `topk_segments=200`、视频级聚合和 FAISS flat inner-product 索引条件下进行 1kA q200 评测。  
在系统型增强阶段，rewrite 实验主要比较 `rewrite all` 与 `rewrite selective` 两类策略，并进一步考察歧义阈值、rewrite rate 与 token 消耗之间的关系；rerank 实验则在固定小规模候选集合上进行语义评分与融合排序。  
在表征增强阶段，系统逐步引入 ViCLIP teacher supervision、alignment teacher、多视角视频特征以及 gated adapter 等策略；在持续学习阶段，则以当前最佳 checkpoint 为 warm start，围绕 `rank 2-30` 的高价值错误样本构建 feedback teacher，并在 `safe_dev` 上进行 acceptance-gated promotion。

> **Table 5-1 建议插入位置：本节末尾**  
> **表名：** Table 5-1 实验协议与主要参数设置表

### Table 5-1 实验协议与主要参数设置表
| 类别              | 设置                         | 说明                               |
| ----------------- | ---------------------------- | ---------------------------------- |
| 数据集            | MSR-VTT                      | 统一实验数据集                     |
| 学习协议          | safe_train                   | 用于 teacher 构建与 candidate 学习 |
| 选模协议          | safe_dev                     | 用于 continual loop 验证与晋升判断 |
| 汇报协议          | 1kA                          | 用于最终锁定结果汇报               |
| 基座模型          | ViT-H-14 / laion2b_s32b_b79k | 统一视觉语言底座                   |
| baseline 评测规模 | q200                         | 便于与既有阶段结果一致比较         |
| 候选召回设置      | topk_segments=200            | baseline 主设置                    |
| 候选优化范围      | Top-K small candidate set    | 控制时延与成本                     |
| 持续学习初始化    | warm-start checkpoint        | 从当前 best 出发训练 candidate     |
| 持续学习筛样      | rank 2-30 near-failure       | 构建高价值 feedback teacher        |

---

## 5.2 Baseline 检索实验结果分析

### 5.2.1 不同 pooling 方法与聚合策略对比

Baseline 阶段的首要任务是确定一个稳定、可复现且具有较好候选召回能力的检索底座。由于视频最终返回对象是视频级结果，因此片段级响应如何聚合为视频级得分，会直接影响最终排序质量。本文在早期实验中对不同 pooling 与聚合策略进行了比较，结果表明，稳定的 mean/聚合路线能够为后续增强提供更可靠的基准结果，而过度依赖局部强响应的策略虽然在个别查询上可能更敏感，但整体波动更大。

这一现象与 Video-ColBERT 等工作中的观察具有一致性，即视频检索若希望在后续引入更细粒度的 late interaction 或局部比较，前端的全局表示和候选召回层必须先保持稳定[13]。因此，本文并未将 baseline 设计成高度激进的局部交互结构，而是优先保留一个较稳的全局召回底座。

### 5.2.2 不同 Top-K 设置与候选召回分析

在 baseline 之外，本文进一步分析了不同 Top-K 设置对候选召回能力的影响。从 `baseline_vith14_candidate_recall_top30_1kA_full.json` 可以看到，在 `candidate_topk=30` 的条件下，系统在 `1kA full` 上的候选召回结果为：`R@1=39.0, R@5=61.86, R@10=71.24, R@30=83.78`。这说明：对于大多数查询，正确视频在初始召回阶段已经能够进入较小范围候选集合之中，但并未总能被稳定排到首位。

这一现象非常重要。它说明本文后续性能瓶颈并不主要在“候选完全找不到”，而更多集中在“候选内部排序不够精确”。VideoComp 的结果同样指出，视频文本模型在面对组合语义和时间敏感描述时，常常不是缺少相关候选，而是缺少对 subtle temporal / compositional differences 的稳定判断能力[14]。因此，后续继续优化排序与细粒度对齐是有理论和实验依据的。

### 5.2.3 Baseline 检索性能分析

在 1kA q200 锁定评测条件下，baseline 主结果为：

- \(R@1=47.5\)
- \(R@5=65.0\)
- \(R@10=72.0\)
- MedR = 2.0
- MnR = 17.32

这一结果说明 baseline 已具备较好的文本到视频检索能力，能够支撑后续多阶段增强实验。与此同时，结合 candidate recall 结果可以发现，baseline 的问题并不是整体语义完全失效，而是在 top1 校准能力上仍有提升空间。TokenBinder 与 Beyond Coarse-Grained Matching 的研究均指出，这类“整体相关、细节不准”的误差是当前视频文本检索任务中的常见瓶颈[8][9]。因此，本文后续的 rewrite、rerank 以及 alignment / multiview 增强，均围绕这一点展开。

### 5.2.4 Baseline 时延分析

从 `baseline_vith14_mean_topk200_q200.json` 可以看到，baseline 在 q200 下总耗时约为 3.78 秒，平均每条查询总检索时间约为 18.61 ms，其中 search 阶段平均 18.53 ms，聚合阶段平均 0.078 ms。  
这一结果说明，baseline 路径在本地单机配置下已经具有较好的在线响应能力，足以承担系统底座角色。也正因为 baseline 本身足够轻量，后续 rewrite 与 rerank 所引入的额外成本才能被清楚观察和比较，而不会被底座本身的高延迟掩盖。

> **Figure 5-1 建议插入位置：5.2 节末尾**  
> **图名：** Figure 5-1 Baseline 检索性能与候选召回分析图

### Figure 5-1 绘图代码
```powershell
@'
import json
from pathlib import Path
import matplotlib.pyplot as plt

base = json.loads(Path("outputs/tables/analysis/baseline_vith14_mean_topk200_q200.json").read_text(encoding="utf-8"))
cand = json.loads(Path("outputs/tables/analysis/baseline_vith14_candidate_recall_top30_1kA_full.json").read_text(encoding="utf-8"))

metrics = base["metrics"]
crec = cand["candidate_recall"]

labels = ["R@1", "R@5", "R@10"]
baseline_vals = [metrics["R@1"], metrics["R@5"], metrics["R@10"]]
candidate_vals = [crec["R@1"], crec["R@5"], crec["R@10"]]

plt.figure(figsize=(8, 5))
x = range(len(labels))
plt.plot(x, baseline_vals, marker="o", linewidth=2, label="Baseline 1kA q200")
plt.plot(x, candidate_vals, marker="s", linewidth=2, label="Candidate Recall Top30 (1kA full)")
plt.xticks(list(x), labels)
plt.ylabel("Recall (%)")
plt.title("Figure 5-1 Baseline Performance and Candidate Recall")
plt.legend()
plt.grid(alpha=0.25)
Path("outputs/figures").mkdir(parents=True, exist_ok=True)
plt.tight_layout()
plt.savefig("outputs/figures/Figure5_1_baseline_candidate_recall.png", dpi=220)
print("saved: outputs/figures/Figure5_1_baseline_candidate_recall.png")
'@ | python -
```

---

## 5.3 系统型增强实验分析

### 5.3.1 Rewrite All 与 Baseline 对比

在 rewrite all 设置下，系统试图对所有输入查询统一进行大语言模型语义补充。实验观察表明，该策略虽然能够在部分含混 query 上带来额外语义信息，但整体收益并不稳定。其原因在于：对于原本已足够清晰的查询，统一改写容易引入冗余或偏移语义，从而削弱原始检索向量与目标视频之间的直接对应关系。这一点与基于生成模型的 query expansion 研究中的普遍观察是一致的，即生成式增强应受到触发条件和成本控制的约束，而不应默认全量启用[11]。

### 5.3.2 Rewrite Selective 与 Baseline 对比

与 rewrite all 相比，selective rewrite 只在查询存在明显歧义或表达不完整时才触发。根据当前 1kA q200 对比结果，baseline 为 `47.5 / 65.0 / 72.0`，而较优的 selective rewrite 结果达到 `48.0 / 67.5 / 73.5`。这说明：当 rewrite 被限定在真正有必要的查询上时，系统能够在不显著增加无效调用的情况下带来可见收益。  
这种结果与 NarVid 中“额外语言信息只有在被查询感知地利用时才更可能稳定带来提升”的结论相一致[15]。也就是说，增强本身并不是越多越好，而是要与当前检索需求形成匹配。

### 5.3.3 Rewrite + Rerank 组合效果分析

在当前实验中，rewrite 与 rerank 组合并未带来超过最佳 selective rewrite 的收益。已有 1kA q200 对比结果显示，rewrite+rerrank 约为 `48.0 / 65.0 / 72.5`，其中 \(R@1\) 与 selective rewrite 相当，但 \(R@5\) 和 \(R@10\) 没有进一步提高。这说明，当前 rerank 路径虽然在局部查询上能够改善排序，但整体上还不足以稳定推动 locked 1kA 主指标。

造成这一现象的原因主要有两点。第一，候选优化本质上依赖 baseline 候选覆盖质量，若候选集合内部已存在较强的相似性偏置，则 rerank 容易只做局部微调而难以改变整体排序结构[10]。第二，若系统误差本质上来自视频表示端的细粒度语义不足，仅依赖文本侧改写和候选端重排，很难从根本上解决问题[6][8]。

### 5.3.4 改写比例、时延与 token 成本分析

系统型增强的一大现实问题是成本。selective rewrite 的优势不只体现在 recall 小幅提升，更体现在其通过减少不必要的 LLM 调用有效控制了 token 消耗和响应时间。相比 rewrite all，selective rewrite 能够在更低调用频率下保留主要收益，因此更符合工程系统的部署需求。  
综合来看，rewrite 和 rerank 在本文中都属于“有效但有限”的系统型增强：它们有工程意义、有实验价值，但不是最终主要性能来源。论文中对其最合理的表述方式，是将它们作为系统分层优化链路的前端和中端增强模块，而不是全文主创新。

> **Figure 5-2 建议插入位置：5.3 节末尾**  
> **图名：** Figure 5-2 Baseline、Selective Rewrite 与 Rewrite+Rerank 对比图

### Figure 5-2 绘图代码
```powershell
@'
from pathlib import Path
import matplotlib.pyplot as plt
import numpy as np

labels = ["Baseline", "Selective Rewrite", "Rewrite+Rerank"]
r1 = [47.5, 48.0, 48.0]
r5 = [65.0, 67.5, 65.0]
r10 = [72.0, 73.5, 72.5]

x = np.arange(len(labels))
w = 0.22

plt.figure(figsize=(9, 5))
plt.bar(x - w, r1, width=w, label="R@1")
plt.bar(x, r5, width=w, label="R@5")
plt.bar(x + w, r10, width=w, label="R@10")
plt.xticks(x, labels, rotation=10)
plt.ylabel("Recall (%)")
plt.title("Figure 5-2 Comparison of Baseline, Selective Rewrite and Rewrite+Rerank")
plt.legend()
plt.grid(axis="y", alpha=0.2)
Path("outputs/figures").mkdir(parents=True, exist_ok=True)
plt.tight_layout()
plt.savefig("outputs/figures/Figure5_2_rewrite_rerank_comparison.png", dpi=220)
print("saved: outputs/figures/Figure5_2_rewrite_rerank_comparison.png")
'@ | python -
```

---

## 5.4 细粒度表征增强实验分析

### 5.4.1 表征增强实验设计与阶段划分

在系统型增强收益趋于有限之后，本文将重点转向表征增强。实验路径大致经历了三个阶段：  
第一阶段为 teacher supervision 与基础 adapter 蒸馏；第二阶段引入 alignment teacher，直接为局部语义对齐提供附加监督；第三阶段进一步加入 multiview features，对视频侧表示进行补充。  
这一演进路线与近年来视频文本检索从 coarse-grained matching 走向 fine-grained compositional and temporal alignment 的趋势一致[13][14][15][16]。

### 5.4.2 Alignment Teacher 的增益分析

在 locked 1kA 条件下，alignment teacher 的引入带来了稳定的 top1 改善。以 `stage3_alignment_teacher_f6_1kA_train500_eval200_gpu_v1` 到 `stage3_alignment_teacher_f6_gate_1kA_train500_eval200_gpu_v2` 再到 `stage3_alignment_teacher_train_targets_1kA_f6_v33` 的实验链路为例，可以看到：系统逐步把排序能力从更偏全局的匹配，转向了更强调局部动作、对象与关系对应的对齐增强。

这一结果符合 DiscoVLA 所强调的观点：视频文本检索的迁移困难不只来自视觉表示本身，还来自语言表达和跨模态对齐差异[6]。因此，alignment teacher 的作用不是简单“多一个 loss”，而是让 student 学到更接近检索瓶颈本身的局部语义监督。

### 5.4.3 Multiview Features 的增益分析

在 alignment teacher 基础上，multiview features 进一步将 locked 1kA 的最佳结果推进到当前主结果。`stage3_alignment_multiview_v35_1kA_train500_eval200_gpu_v1` 显示：

- baseline：`49.5 / 65.0 / 74.0`
- adapter：`50.5 / 64.5 / 73.5`

从数值上看，adapter 将 \(R@1\) 提升到了当前 locked 1kA 最优 `50.5`。这说明，当系统的主要瓶颈集中在 top1 排序而非广义候选覆盖时，多视角局部补充确实能够带来更稳定的首位判别增益。VideoComp、SF2T 以及 NarVid 等近期研究都从不同角度说明了相似问题：细粒度视频理解与检索需要对局部事件、帧级信息和复合语义保持足够敏感，而不能只依赖单一路径的整体表示[14][15][16]。

### 5.4.4 Locked 1kA 主结果分析

从整体结果看，本文当前 locked 1kA 最优结果来自 `alignment + multiview` 路线，而不是 rewrite / rerank 或持续学习候选模型。这一点非常关键，因为它说明论文的**主创新**不应写成“把很多模块堆起来”，而应明确聚焦为“细粒度对齐增强带来的 top1 校准能力提升”。  
进一步地，`best_method=adapter` 而非后续的 `fused88/90/92`，说明真正有效的增益来自 student 表示本身的改善，而不是简单的后处理分数融合。这对于论文的算法贡献表述是很有利的。

> **Figure 5-3 建议插入位置：5.4 节末尾**  
> **图名：** Figure 5-3 从 Baseline 到 Stage3 最优模型的阶段性提升图

### Figure 5-3 绘图代码
```powershell
@'
import json
from pathlib import Path
import matplotlib.pyplot as plt

files = [
    ("Baseline", "outputs/tables/analysis/baseline_vith14_mean_topk200_q200.json", "metrics"),
    ("Stage3 Best\n(v3.5)", "outputs/tables/analysis/stage3_alignment_multiview_v35_1kA_train500_eval200_gpu_v1.json", "methods.adapter"),
    ("Err-Driven\n1kA Eval", "outputs/tables/analysis/error_driven_round01_1kA_eval.json", "methods.adapter"),
]

def get_metrics(path, key):
    data = json.loads(Path(path).read_text(encoding="utf-8"))
    if key == "metrics":
        return data["metrics"]
    if key == "methods.adapter":
        return data["methods"]["adapter"]
    raise ValueError(key)

labels, r1, r5, r10 = [], [], [], []
for label, path, key in files:
    m = get_metrics(path, key)
    labels.append(label)
    r1.append(m["R@1"])
    r5.append(m["R@5"])
    r10.append(m["R@10"])

x = range(len(labels))
plt.figure(figsize=(8, 5))
plt.plot(x, r1, marker="o", linewidth=2, label="R@1")
plt.plot(x, r5, marker="s", linewidth=2, label="R@5")
plt.plot(x, r10, marker="^", linewidth=2, label="R@10")
plt.xticks(list(x), labels)
plt.ylabel("Recall (%)")
plt.title("Figure 5-3 Stage-wise Improvement on Locked 1kA")
plt.legend()
plt.grid(alpha=0.25)
Path("outputs/figures").mkdir(parents=True, exist_ok=True)
plt.tight_layout()
plt.savefig("outputs/figures/Figure5_3_stage_progression.png", dpi=220)
print("saved: outputs/figures/Figure5_3_stage_progression.png")
'@ | python -
```

> **Table 5-2 建议插入位置：5.4.4 后**  
> **表名：** Table 5-2 主要阶段结果对比表

### Table 5-2 主要阶段结果对比表
| 方法阶段                    | 协议     |  R@1 |  R@5 | R@10 | MedR |    MnR | 说明                                |
| --------------------------- | -------- | ---: | ---: | ---: | ---: | -----: | ----------------------------------- |
| Baseline                    | 1kA q200 | 47.5 | 65.0 | 72.0 |  2.0 |  17.32 | 基础检索底座                        |
| Selective Rewrite           | 1kA q200 | 48.0 | 67.5 | 73.5 |  2.0 |  16.67 | 系统型增强                          |
| Rewrite + Rerank            | 1kA q200 | 48.0 | 65.0 | 72.5 |  2.0 |  16.80 | 局部有效但整体有限                  |
| Stage3 Best (v3.5) baseline | 1kA q200 | 49.5 | 65.0 | 74.0 |  2.0 | 20.365 | 增强底座                            |
| Stage3 Best (v3.5) adapter  | 1kA q200 | 50.5 | 64.5 | 73.5 |  1.0 |  20.42 | 当前 locked 主结果                  |
| Error-driven candidate      | 1kA q200 | 48.5 | 66.5 | 74.0 |  2.0 | 20.075 | safe_dev 有效，但未超过 locked best |

---

## 5.5 错误驱动自学习闭环实验分析

### 5.5.1 Failure Diagnosis 与反馈样本质量分析

为了验证系统是否具备持续优化潜力，本文首先基于 `failure_diagnosis_v35_1kA_q200.json` 对当前最佳模型的失败结构进行统计。结果显示，在 200 条 locked 1kA 查询中：

- top1 命中 99 条
- `top2_5` 有 33 条
- `top6_10` 有 15 条
- `top11_30` 有 31 条
- `gt_outside_top30` 有 22 条

从失败类型分布看，错误主要集中在：

- `object = 72`
- `relation = 67`
- `person_attribute = 53`
- `action = 43`

而 `temporal_order = 3` 相对较少。这个结果很关键，因为它说明当前瓶颈并不主要是长时序复杂推理，而更集中在对象、关系与人物属性的细粒度区分上。这也从反面印证了本文将主要创新聚焦在 fine-grained alignment 上是合理的。类似地，VideoComp、Video-ColBERT 与 SF2T 等工作也强调，细粒度组合关系和局部视频片段建模对视频文本匹配性能具有决定性影响[13][14][16]。

### 5.5.2 Safe-dev 闭环增益分析

在 safe_dev 上，错误驱动闭环显示出正向收益。根据 `reference_current_best_eval.json` 与 `round01_summary.json` 对比，reference adapter 为：

- `R@1=40.0`
- `R@5=60.0`
- `R@10=71.5`

而 round01 candidate adapter 达到：

- `R@1=40.5`
- `R@5=62.5`
- `R@10=72.0`

也就是说，safe_dev 上出现了：

- `R@1 +0.5`
- `R@5 +2.5`
- `R@10 +0.5`

这说明：在高价值错误样本筛选、feedback teacher 构建和 warm-start 学习路径都成立的情况下，系统确实具备从错误中进一步吸收有效监督的能力。C-CLIP 与 Mind the Gap 的研究都指出，视觉语言模型的 continual learning 必须同时考察 forward transfer、matching performance 与 forgetting，而不能只看局部新任务收益[12][17]。从这一角度看，本文的 safe_dev 闭环已经具备“初步成立”的证据。

### 5.5.3 Locked 1kA 验证结果分析

然而，当 safe_dev 上通过门控的 candidate 被重新放回 locked 1kA 评测时，其结果为：

- baseline：`48.0 / 65.5 / 72.5`
- adapter：`48.5 / 66.5 / 74.0`

尽管该 candidate 在 \(R@5\) 和 \(R@10\) 上表现不差，但其 \(R@1\) 仍未超过当前主结果 `50.5`。因此，这一轮闭环不能被视为新的最终主模型晋升结果。  
这说明当前 continual loop 已经证明了“safe_dev 上可产生正向增益”，但还没有证明“该增益能稳定迁移到 locked 1kA”。从论文角度看，这反而是一个重要发现：**错误驱动自学习闭环已具备局部有效性，但其泛化稳定性仍受限于反馈样本质量和协议间分布差异。**

### 5.5.4 为什么持续学习增益尚未稳定迁移

综合分析，当前 continual loop 未能稳定超越 locked 主结果，主要有三点原因。  
第一，当前 high-value failure 仍主要依赖当前系统结果分布，其 teacher 质量受限于当前 student 已有表征能力。第二，safe_dev 上有效的局部修正，未必能直接迁移到 locked 1kA 的分布上。第三，当前候选学习更多地改善了 \(R@5\)/\(R@10\) 与 MnR，而并未稳定推动 \(R@1\) 超越现有 best，这说明它更偏“广义召回修正”而非“强 top1 校准”。  
因此，在论文写作中，最合理的表述是：**本文验证了错误驱动 continual loop 的可行性，但当前最稳的主结果仍来自细粒度表征增强，而非后续自学习闭环。**

> **Figure 5-4 建议插入位置：5.5.2 后**  
> **图名：** Figure 5-4 Safe-dev 闭环前后结果对比图

### Figure 5-4 绘图代码
```powershell
@'
import json
from pathlib import Path
import matplotlib.pyplot as plt

ref = json.loads(Path("outputs/tables/analysis/error_driven_agent_loop/20260424_163109_error_driven_safe_dev/reference_current_best_eval.json").read_text(encoding="utf-8"))
cand = json.loads(Path("outputs/tables/analysis/error_driven_agent_loop/20260424_163109_error_driven_safe_dev/round01_summary.json").read_text(encoding="utf-8"))

labels = ["Reference", "Candidate"]
r1 = [ref["methods"]["adapter"]["R@1"], cand["methods"]["adapter"]["R@1"]]
r5 = [ref["methods"]["adapter"]["R@5"], cand["methods"]["adapter"]["R@5"]]
r10 = [ref["methods"]["adapter"]["R@10"], cand["methods"]["adapter"]["R@10"]]

x = range(len(labels))
plt.figure(figsize=(7, 5))
plt.plot(x, r1, marker="o", linewidth=2, label="R@1")
plt.plot(x, r5, marker="s", linewidth=2, label="R@5")
plt.plot(x, r10, marker="^", linewidth=2, label="R@10")
plt.xticks(list(x), labels)
plt.ylabel("Recall (%)")
plt.title("Figure 5-4 Safe-dev Gain of Error-driven Continual Loop")
plt.legend()
plt.grid(alpha=0.25)
Path("outputs/figures").mkdir(parents=True, exist_ok=True)
plt.tight_layout()
plt.savefig("outputs/figures/Figure5_4_safe_dev_loop_gain.png", dpi=220)
print("saved: outputs/figures/Figure5_4_safe_dev_loop_gain.png")
'@ | python -
```

> **Table 5-3 建议插入位置：5.5.3 后**  
> **表名：** Table 5-3 持续学习闭环结果对比表

### Table 5-3 持续学习闭环结果对比表
| 比较对象             | 协议     |  R@1 |  R@5 | R@10 | MedR |    MnR | 结论               |
| -------------------- | -------- | ---: | ---: | ---: | ---: | -----: | ------------------ |
| v3.5 current best    | safe_dev | 40.0 | 60.0 | 71.5 |  2.0 | 42.735 | 闭环前参考         |
| error-driven round01 | safe_dev | 40.5 | 62.5 | 72.0 |  2.0 |  40.57 | safe_dev 正向增益  |
| v3.5 current best    | 1kA      | 50.5 | 64.5 | 73.5 |  1.0 |  20.42 | 当前主结果         |
| error-driven round01 | 1kA      | 48.5 | 66.5 | 74.0 |  2.0 | 20.075 | 未超过 locked best |

---

## 5.6 案例分析与误差分析

### 5.6.1 典型成功案例分析

典型成功案例通常表现为：query 中的主体、动作和对象都较清晰，同时视频中的局部证据与文本描述具有较高一致性。在此类场景下，baseline 已能获得较好结果，而 alignment + multiview 路线则进一步提升了 top1 排序的稳定性。这说明当前系统在“粗粒度相关 + 局部语义明确”的场景中已具备较强能力。

### 5.6.2 查询改写有效案例分析

rewrite 的有效场景主要集中在描述过短、语义省略或主体/对象约束不足的 query 上。对这些 query 而言，selective rewrite 能够补充关键词项和上下文信息，使文本向量更接近目标视频语义空间。相比之下，对于本身已经清晰的 query，全量 rewrite 反而容易带来冗余。

### 5.6.3 细粒度增强有效案例分析

细粒度增强最有效的场景，是多个候选都整体相关，但只有目标视频在局部对象、人物属性或关系语义上真正满足查询要求。这类情况下，alignment teacher 和 multiview 特征的作用最明显，因为它们能够补足 baseline 在局部证据和多路径表示上的不足。

### 5.6.4 失败案例与原因分析

从 `failure_diagnosis_v35_1kA_q200.json` 的统计结果看，当前系统的失败主要集中在：

- object
- relation
- person_attribute
- action

而 temporal_order 错误比例较低。  
这说明当前系统并非完全不会处理时间顺序，而是更容易在对象与关系的细粒度差异上发生混淆。换句话说，未来若要继续提升 locked 1kA \(R@1\)，最值得投入的方向不是继续泛化式扩大召回，而是进一步增强对象、关系和属性层面的局部语义建模能力。

> **Figure 5-5 建议插入位置：5.6.4 后**  
> **图名：** Figure 5-5 失败类型分布统计图

### Figure 5-5 绘图代码
```powershell
@'
import json
from pathlib import Path
import matplotlib.pyplot as plt

data = json.loads(Path("outputs/tables/analysis/failure_diagnosis_v35_1kA_q200.json").read_text(encoding="utf-8"))
tags = data["failure_tag_counts"]

labels = list(tags.keys())
values = list(tags.values())

plt.figure(figsize=(9, 5))
plt.bar(labels, values, color="#7aa6c2")
plt.xticks(rotation=25)
plt.ylabel("Count")
plt.title("Figure 5-5 Failure Tag Distribution on Locked 1kA")
plt.grid(axis="y", alpha=0.2)
Path("outputs/figures").mkdir(parents=True, exist_ok=True)
plt.tight_layout()
plt.savefig("outputs/figures/Figure5_5_failure_tags.png", dpi=220)
print("saved: outputs/figures/Figure5_5_failure_tags.png")
'@ | python -
```

---

## 5.7 本章小结

本章围绕实验结果对本文系统进行了系统分析。首先，baseline 实验表明当前系统已经具备较好的高效候选召回能力，但 top1 校准仍存在提升空间；其次，rewrite 与 rerank 证明了系统型增强的有效性，但其整体收益有限，难以构成最终主增益来源；进一步地，alignment teacher 与 multiview features 在 locked 1kA 上带来了当前最稳定的主结果提升，说明细粒度表征增强是本文最重要的方法贡献；最后，错误驱动 continual loop 在 safe_dev 上展示出正向增益，但尚未在 locked 1kA 上稳定超越当前最佳模型，表明持续学习框架具备可行性，但其泛化稳定性仍需进一步提升。整体来看，本文的实验结果与第3章方法设计、第4章系统实现保持一致，较好地支撑了“以细粒度对齐增强为主、以错误驱动闭环为扩展”的研究主线。

---

# 第5章新增引用与链接

下面这 5 篇建议作为第5章新增引用，编号从 `[13]` 开始。

[13] Reddy et al. Video-ColBERT: Contextualized Late Interaction for Text-to-Video Retrieval. CVPR 2025.  
链接：https://openaccess.thecvf.com/content/CVPR2025/html/Reddy_Video-ColBERT_Contextualized_Late_Interaction_for_Text-to-Video_Retrieval_CVPR_2025_paper.html

[14] Kim et al. VideoComp: Advancing Fine-Grained Compositional and Temporal Alignment in Video-Text Models. CVPR 2025.  
链接：https://openaccess.thecvf.com/content/CVPR2025/html/Kim_VideoComp_Advancing_Fine-Grained_Compositional_and_Temporal_Alignment_in_Video-Text_Models_CVPR_2025_paper.html

[15] Hur et al. Narrating the Video: Boosting Text-Video Retrieval via Comprehensive Utilization of Frame-Level Captions. CVPR 2025.  
链接：https://openaccess.thecvf.com/content/CVPR2025/html/Hur_Narrating_the_Video_Boosting_Text-Video_Retrieval_via_Comprehensive_Utilization_of_CVPR_2025_paper.html

[16] Hu et al. SF2T: Self-supervised Fragment Finetuning of Video-LLMs for Fine-Grained Understanding. CVPR 2025.  
链接：https://openaccess.thecvf.com/content/CVPR2025/html/Hu_SF2T_Self-supervised_Fragment_Finetuning_of_Video-LLMs_for_Fine-Grained_Understanding_CVPR_2025_paper.html

[17] Liu et al. C-CLIP: Multimodal Continual Learning for Vision-Language Model. ICLR 2025 Poster.  
链接：https://openreview.net/forum?id=sb7qHFYwBc

---

# 新增引用建议出现位置

## `[13] Video-ColBERT`
- `5.2.1`：说明稳定全局底座与后续细粒度交互的关系
- `5.6.4`：说明 top1 排序受局部交互质量影响

## `[14] VideoComp`
- `5.2.2`：说明候选已召回但组合/时序判断仍困难
- `5.5.1`：说明细粒度失败类型分析的重要性

## `[15] NarVid`
- `5.3.2`：说明“额外语言信息只有在被合理利用时才更稳定有效”
- `5.4.3`：说明局部描述/帧级补充对检索提升的重要性

## `[16] SF2T`
- `5.4.3`：说明细粒度片段理解与局部视频语义增强的重要性
- `5.6.4`：支撑对象/动作局部误差分析

## `[17] C-CLIP`
- `5.5.2`：说明 continual learning 需要同时看匹配性能与稳定性
- `5.5.4`：说明 safe_dev 增益未必等于 locked split 稳定迁移

如果你愿意，我下一步可以继续直接帮你做两件最有价值的事之一：

1. 把**第4章和第5章所有图统一整理成一套批量出图脚本**
2. 继续写**摘要、引言里的“创新点列表”和全文贡献总结**，把论文口径彻底收束起来